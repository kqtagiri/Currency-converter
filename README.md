SIGMA PROJECT.
Repeat all main themes(HTTP/REST API, SQL(Postres)). In this project people can create users, which can have some wallets with other currency(BYN, EUR, USD). The code takes rates to convert from one currency to another from bank api every day. The history of rates you can check by one of http request. You also can do the transaction to convert from one currency to another.
